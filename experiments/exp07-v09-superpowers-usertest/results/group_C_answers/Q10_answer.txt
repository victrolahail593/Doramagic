注入的知识包中**没有任何关于 `@` 语法引用 skill 的内容**。

你提到"在 Superpowers 文档里看到"，但提供的知识注入文件里没有这个特性的描述。我无法确认这个功能是否存在。

**我能确认的内容：**

根据注入的知识，skill 触发机制是这样的：
- Claude 代理在收到用户消息后，判断是否有相关 skill（哪怕 1% 概率），然后用 `Skill` 工具调用它
- 触发是代理主动判断的，不是文档内容驱动的

**关于你的问题：**

在 brainstorming 的设计文档里写 `@test-driven-development`，能否"自动触发" TDD skill，取决于 superpowers 是否实现了这个解析机制。知识包里没有这个特性，我不能凭空确认它存在。

**建议：**
- 去 superpowers 的 GitHub 仓库查看实际文档或相关 Issue
- 或者直接问"你在用哪个 skill？"来测试行为是否符合预期（知识包里提到这是确认 skill 是否被激活的暂时方法）

你在哪里看到 `@` 语法的描述？如果有具体链接或截图可以进一步确认。
