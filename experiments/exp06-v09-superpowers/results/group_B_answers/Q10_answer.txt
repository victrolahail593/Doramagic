注入的知识包中没有任何关于 `@` 语法的记载。

根据知识注入的内容，skill 的调用机制只有一种：**通过 Skill tool 显式调用**。文档明确区分了两种不同的加载机制（Skill tool vs Read tool），并指出不能混用。

你描述的 `@test-driven-development` 语法在知识包中不存在，没有依据支持这一用法。

几个可能的情况：

1. **你看到的文档可能是第三方内容或过时版本** — 知识包没有提到此特性
2. **可能是混淆了其他工具的语法** — 例如某些 AI 工具有 `@file` 引用语法，但那是文件引用，不是 skill 触发
3. **这个特性不存在** — 根据知识包，skill 触发是代理的主动行为，不是文档里的声明式触发

正确的做法是：代理在 brainstorming 结束后，**主动通过 Skill tool 调用 TDD skill**，而不是在设计文档里写 `@` 引用来"自动触发"。这也符合知识包强调的原则——skill 触发是有意识的工程决策，不是被动的文本匹配。

如果你在某处看到了这个语法，建议提供具体来源以便核实。
