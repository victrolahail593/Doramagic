提供的知识注入文件中**没有提到任何 `@` 语法**用于引用其他 skill。

这个特性很可能不存在，或者你看到的来源值得怀疑。根据知识注入的内容：

- Skills 只能通过 **Skill tool** 调用（Claude Code 环境中）
- 触发机制是 Claude 代理主动判断"是否有 1% 可能性适用某个 skill"，然后用 Skill tool 显式调用
- 没有任何关于文档内 `@mention` 自动触发的描述

**在设计文档里写 `@test-driven-development` 不会自动触发 TDD skill。** 设计文档是给人或代理阅读的文本，不是可执行的指令。

如果你想确保 brainstorming 结束后自动进入 TDD 流程，正确做法是：
1. brainstorming skill 的输出中明确要求代理接下来调用 `superpowers:test-driven-development`
2. 或者在你的指令里写明"完成 brainstorming 后，必须先用 TDD skill 再写代码"

如果你在某个具体文档或来源中看到了 @ 语法的描述，建议分享出来核实——有可能是 AI 幻觉生成的文档。
